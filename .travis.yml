language: python
python:
  - '2.7'
  - '3.6'
before_install:
    - pip install tox --upgrade
    - pip install codecov
script: tox -e $(echo py$TRAVIS_PYTHON_VERSION | tr -d .)
after_success:
    - codecov -F unit functional
cache:
    pip: true 
    directories:
      - $TRAVIS_BUILD_DIR/.tox
git:
  depth: 2147483647
